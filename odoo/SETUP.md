# Odoo Setup

1. `cd odoo/Day1`
2. Clone Odoo here: https://github.com/odoo/odoo (branch `19.0`)
3. `python -m venv .venv19 && .venv19\Scripts\activate && pip install -r requirements.txt`
4. `python odoo-bin -c odoo.conf` — DONE!
